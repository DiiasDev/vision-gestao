# Produção: use URL fixa da API
EXPO_PUBLIC_API_URL=https://api.seu-dominio.com

# Desenvolvimento:
# false = detecta IP automaticamente via host do Metro (recomendado)
# true  = usa EXPO_PUBLIC_API_URL fixo
EXPO_PUBLIC_USE_FIXED_API_URL=false
